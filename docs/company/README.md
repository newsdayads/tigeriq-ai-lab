# Repository company-source copies

The files in this directory are **repository mirrors / historical reference copies**. They are not the live ChatGPT Project Source and are not runtime/current-state authority.

Rules:
- Never infer precedence from `_v1`, file timestamps, worktree age, or repository location.
- Explicit current Owner instruction and the Project Bootstrap loaded by ChatGPT retain the precedence defined by the current Constitution / Workflow / Source Index.
- Dynamic operational state belongs to CENTRAL #280 and the dynamic sources it points to, including Interaction #504 and Registry #335.
- Use `docs/HOT_STATE.json` only as a derived fast-start index; follow its pointers and deep-read the authoritative source whenever stale/conflicting or when fresh evidence is required.
- Old worktrees/releases/snapshots remain rollback/evidence only and must not participate in current-state resolution.
