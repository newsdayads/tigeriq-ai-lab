# WO-047 Milestone 1 Evidence — API/Schema Contract

Date: 2026-08-31  
Branch: `wo047/api-first-inference-gateway`  
Base: PR #111 exact head `1f8261c59b6406a471226a762a3b724d5dad93dd`  
Issue: #125

## Milestone result
PASS — contract-first artifact exists before runtime implementation.

## Contract artifacts
- `docs/contracts/AI_INFERENCE_GATEWAY_V1.md`
- `schemas/ai-inference-gateway-v1.schema.json`

## Consumer boundary
02 APP and 06 Work Management have a shared request/response/auth/evidence/error contract without requiring either stream to modify its implementation during WO-047.

## Security invariants captured
- TigerIQ employee identity is separate from provider/model backend identity.
- Provider API credentials are server-only and are never returned to clients.
- Device calls use a short-lived TigerIQ session token minted only after bootstrap authentication.
- Default token TTL 5 minutes; maximum 15 minutes.
- Gemini + Groq primary tier; OpenRouter bounded fallback.
- Reviewer/Judge independence is expressed as `routing.requiredDistinctFrom` backend identities.
- Retry route cap is 3.
- Error/evidence contracts are sanitized and exclude upstream secret/raw credential values.

## Runtime truth boundary
This milestone is contract evidence only. It does not claim that live provider credentials are configured or that a live cloud inference call has occurred.