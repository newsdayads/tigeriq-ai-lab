# #532 Handoff — Planner + Orchestrator Direct Node

## 1. Mục tiêu cuối
- Loại PowerShell bootstrap khỏi hot-path Planner/Orchestrator.
- Scheduled Task phải chạy `node.exe` trực tiếp, giữ self-heal, state, queue và rollback.
- Không chạm AI provider/router #529, Web V5, OpenClaw/browser owner hoặc NV02 Auto Worker.

## 2. Yêu cầu đã xác nhận
- Canonical runtime root: `D:\TigerIQ`.
- Controller + Native Worker đã direct Node từ #530; Planner/Orchestrator là lớp shell thừa kế tiếp.
- Wrapper PowerShell chỉ được giữ làm recovery/rollback artifact.

## 3. Quyết định đã chốt
- Branch: `issue-532-planner-orchestrator-direct-node`.
- Worktree: `D:\TigerIQ\worktrees\issue-532-planner-orchestrator-direct-node`.
- Runtime action: Scheduled Task → `node.exe` trực tiếp.
- Node tự ghi log; không phụ thuộc shell redirection.

## 4. Cách thực hiện
- Chuyển default path F: → D: trong Planner/Orchestrator standalone.
- Giữ token đọc từ file hiện hành; không lộ/mutation credential.
- Backport contract `route=groq` đang chạy thật từ runtime dist về source để loại source/runtime drift.
- Build exact branch rồi trỏ hai Scheduled Task vào dist của worktree.

## 5. Trạng thái thực tế
- Head hiện hành: `b35c62c7df3e319f67f814cbc44553ea84cf47f8`.
- Typecheck PASS; build PASS; targeted tests 14/14 PASS.
- Planner task Running, PID mới sau kill/recovery: `14340`.
- Orchestrator task Running, PID `32332`.
- Planner `lastCycleAt=2026-09-09T08:52:35.783Z`; mission `lastCycleAt=2026-09-09T08:52:34.613Z`.
- Planner kill/recovery đã có bằng chứng tự phục hồi; không start tay.

## 6. Bằng chứng
- #530 đã closed/completed trước khi claim #532.
- Source commit direct Node/logging/groq-backport: `b35c62c...`.
- Targeted Vitest: 4 files / 14 tests PASS.
- Live state sau kill: Planner task `Running`, PID `14340`, cycle timestamp tiếp tục tăng.
- Orchestrator `Running`, cycle timestamp tiếp tục tăng.

## 7. Lỗi / nguyên nhân / cách sửa
1. Orphan Node từ wrapper cũ làm Supervisor tưởng Planner khỏe dù task mới `Ready` → dọn orphan, retest.
2. Direct Node ban đầu đứng chu kỳ → giả thuyết stdout/stderr; thêm internal log để loại shell redirection.
3. Log lộ lỗi thật `INVALID_ROUTE` → source sạch thiếu `groq` trong khi runtime dist đang hỗ trợ; backport đúng runtime contract + test.

## 8. Không làm lại
- Không làm lại #530 B1–B5; đã PASS và closed.
- Không quay Planner/Orchestrator về PowerShell hot-path trừ rollback sự cố.
- Không sửa provider selection/failover/router #529 trong #532.

## 9. Blocker/quyền Owner
- Không có Owner blocker tại checkpoint này.
- Không reboot, không MAIN/Production, không billing/PAYG, không credential/security mutation.

## 10. Bước tiếp theo
1. Kill/recovery Orchestrator an toàn, xác minh tự phục hồi không start tay.
2. Kiểm tra singleton Planner/Orchestrator và loại false-positive shell count.
3. Xác minh Controller/Worker/Web/OpenClaw không regression.
4. Ghi final evidence, cập nhật #532/#318 và close #532 nếu toàn bộ P1.1–P1.6 PASS.

## 11. Điều kiện hoàn tất
Chỉ close #532 khi cả Planner + Orchestrator direct Node Running, kill/recovery PASS, state tiếp tục, singleton/no hot-path PowerShell PASS, regression core PASS và evidence/Source of Truth đã cập nhật.

STATE: `CHECKPOINT_SAVED_PLANNER_RECOVERY_PASS_ORCHESTRATOR_RECOVERY_PENDING`

## 12. Final verification — 2026-09-09
- Orchestrator PID `32332` bị kill an toàn; ngay sau kill task=`Ready`, process=0, `lastCycleAt=2026-09-09T08:58:23.200Z`.
- Không start tay. Supervisor/Task Scheduler tự phục hồi Orchestrator thành PID `35984`; task=`Running`; mission cycle tiếp tục tới `2026-09-09T09:02:55.511Z` và sau đó tiếp tục tăng.
- Planner vẫn `Running`, PID `14340`, cycle tiếp tục tăng.
- Final singleton: đúng 2 Node process canonical cho Planner + Orchestrator; mỗi component 1 instance.
- Scheduled Task của cả hai chạy `C:\Program Files\nodejs\node.exe` trực tiếp vào dist của branch #532.
- `legacyShellCount=0` cho wrapper `run-autonomous-planner-v1.ps1` / `run-mission-orchestrator-v1.ps1` trong normal runtime path.
- Controller `ok=true`, PostgreSQL `true`, PC01 online `true`.
- Web Control private bind `100.97.23.87:8787`; `/api/server` trả `truthSource=workforce-controller-v1`, `available=true`, Controller/PC01 online.
- OpenClaw listener `127.0.0.1:18789` vẫn đúng 1 listener.
- Typecheck PASS; build PASS; targeted Vitest 4 files / 14 tests PASS.

## 13. Acceptance P1.1–P1.6
- P1.1 Planner direct Node + cycle: PASS.
- P1.2 Orchestrator direct Node + cycle: PASS.
- P1.3 Kill/recovery không start tay: PASS cho Planner và Orchestrator.
- P1.4 Queue/state + Controller/Worker/Web/OpenClaw không regression: PASS trong phạm vi kiểm chứng runtime.
- P1.5 Singleton + no PowerShell hot-path: PASS.
- P1.6 Tests/build/live evidence/Source of Truth: PASS.

STATE: `ISSUE_532_FINAL_PASS_DIRECT_NODE_SELF_HEAL_SINGLETON_NO_SHELL_HOT_PATH`
