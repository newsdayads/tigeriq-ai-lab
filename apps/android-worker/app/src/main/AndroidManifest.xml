<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- TigerIQ V1 independent phone AI employee: no dangerous runtime permission. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:allowBackup="false"
        android:label="TigerIQ AI Lab · Worker V1"
        android:resizeableActivity="true"
        android:theme="@style/TigerIQTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".v07.V07EnrollmentActivity"
            android:exported="true"
            android:resizeableActivity="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".v07.V07StatusActivity"
            android:exported="false"
            android:resizeableActivity="true" />

        <activity
            android:name=".v07.V1AiSetupActivity"
            android:exported="false"
            android:resizeableActivity="true" />

        <receiver
            android:name=".v07.V07BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Frozen/absent from V1 execution: AccessibilityService, overlay, package queries,
             foreground UI automation, banking access, Gemini-app automation and FCM dependency. -->
    </application>
</manifest>
