<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- v0.7 thin-worker minimum permission surface. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <supports-screens
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true" />

    <application
        android:allowBackup="false"
        android:label="TigerIQ AI Worker"
        android:resizeableActivity="true"
        android:theme="@style/TigerIQTheme"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- The only launcher in v0.7: API-first Employee/Device enrollment. -->
        <activity
            android:name=".v07.V07EnrollmentActivity"
            android:exported="true"
            android:resizeableActivity="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".v07.V07StatusActivity"
            android:exported="false"
            android:resizeableActivity="true" />

        <!-- FCM is wake-only. It carries no provider/session/lease secret. -->
        <service
            android:name=".v07.TigerIqMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Re-enqueue authoritative recovery after reboot/app update. -->
        <receiver
            android:name=".v07.V07BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Intentionally absent in v0.7: AccessibilityService, overlay, package queries,
             foreground controller service, battery-optimization bypass and AI-app automation. -->
    </application>
</manifest>
